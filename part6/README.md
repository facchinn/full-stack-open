# Parte 6 — gestión de estado avanzada

La misma aplicación puede ejecutarse con Redux Toolkit (`createSlice`, thunks, filtros y notificaciones) o con TanStack Query + Context. Ejecuta JSON Server con `npm run server`.
