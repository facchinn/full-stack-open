# Parte 4 — Blog list backend

API de blogs con modelos relacionados de Mongoose, creación de usuarios, hash de contraseñas, JWT, autorización por propietario, comentarios y pruebas unitarias de `listHelper`.

Configura `.env` a partir de `.env.example` y ejecuta `npm run dev`.
