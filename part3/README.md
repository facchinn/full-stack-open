# Parte 3 — Phonebook backend

API REST completa con Express, validación de Mongoose, middleware, logging, manejo de errores y pruebas. Si `MONGODB_URI` no está definido usa un repositorio en memoria para desarrollo y tests; al definirlo persiste en MongoDB.

```bash
cp .env.example .env
npm run dev
npm test
```
